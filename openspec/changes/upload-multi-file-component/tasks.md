# Tasks: 通用多文件上传组件

1. 新建 `ImageMultiUpload.vue` 组件，实现多图片上传、预览、删除，支持移动端（参考 global/spec.md 响应式规范）
2. 组件支持 `v-model` 绑定图片URL数组，`max-count=20`，`max-size=10M`，`accept=image/*`
3. 合同签署等页面替换原有证件上传逻辑，集成新组件，移除证件类型、正反面等相关字段和UI
4. 后端接口参数增加 `scene` 区分业务，按需调整存储路径
5. 测试移动端和桌面端上传、预览、删除功能
6. 编写组件和页面使用文档
